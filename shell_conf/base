# Rbenv
if [ -d "$HOME/.rbenv" ]; then
  RBENV_BIN="$HOME/.rbenv/bin"

  case ":$PATH:" in
    *":$RBENV_BIN:"*) :;;
    *) PATH="$RBENV_BIN:$PATH";;
  esac

  eval "$(rbenv init -)"
fi

# OS aliases
alias l="ls -l"
alias la="ls -alh"
alias cls="clear"
